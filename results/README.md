# Results

The committed Petaling Jaya CSV and PNG were extracted from the original uploaded
notebook. They were generated **before** weather and public-transport features were
added to the enriched feature matrix, so they are retained as historical artifacts only.

Rerun `notebooks/02_train_evaluate_rankers.ipynb` for every city to generate the final
all-context results under:

```text
PROJECT_ROOT/modeling_results/<City>/next_poi_3ml_all_context_v2/
```
